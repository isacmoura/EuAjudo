<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>EuAjudo || Dashboard da organização</title>

        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;700&display=swap" rel="stylesheet">

        <link rel="stylesheet" href="../styles/user-dashboard.css">    
    </head>

    <body>
        <div id="main-container">
            <header>
                <span>Bem vinda, <a href="/org/profile"><%= org.name %></a></span>
                <a class="btn-case" href="/org/case">Cadastrar ação</a>
                <a class="btn-logout" href="/sessions/logout">Sair</a>
            </header>

            <h1>Visualize suas ações cadastradas</h1>

            <ul>
                <% cases.forEach(function(c) { %>
                    <li>
                        <strong>Ação:</strong>
                        <p><%= c.title %></p>
        
                        <strong>Descrição:</strong>
                        <p><%= c.description %></p>
        
                        <a href="/delete/case/<%=c.id%>" class="btn-delete">Excluir ação</a>
                    </li>
                <% }); %>
            </ul>
        </div>
    </body>
</html>